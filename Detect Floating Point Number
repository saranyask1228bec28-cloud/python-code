# Enter your code here. Read input from STDIN. Print output to STDOUT
import re
t = int(input())
pattern = r'^[+-]?[0-9]*\.[0-9]+$'

for _ in range(t):
    s = input()
    
    print(bool(re.match(pattern, s)))
